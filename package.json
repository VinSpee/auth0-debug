{
  "name": "auth-test",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://recondite-lake.surge.sh/",
  "dependencies": {
    "auth0-lock": "11.2.3",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-router-dom": "4.2.2",
    "react-scripts": "1.1.1",
    "surge": "0.19.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && cp build/index.html build/404.html && cp build/index.html build/200.html",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "1.1.0"
  }
}
